<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
		"http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>

	<!-- configuration file -->
	<properties resource="jdbc.properties"></properties>

	<!-- mysql driver package -->
	<classPathEntry location="F:\applications\apache-maven-3.3.9\repository\mysql\mysql-connector-java\5.1.34\mysql-connector-java-5.1.34.jar" />

	<context id="context" targetRuntime="MyBatis3">

		<!-- remove all comment -->
		<commentGenerator>
			<property name="suppressAllComments" value="true" />
		</commentGenerator>

		<!-- database connection -->
		<jdbcConnection driverClass="${jdbc.driver}"
			connectionURL="${jdbc.url}"
			userId="${jdbc.username}"
			password="${jdbc.password}" />

		<!-- generate model -->
		<javaModelGenerator targetPackage="yiheng.chen.model" targetProject="src/main/java" />

		<!-- generate MapperXML -->
		<sqlMapGenerator targetPackage="yiheng.chen.mapper" targetProject="src/main/java" />

		<!-- generate Mapper interface -->
		<javaClientGenerator targetPackage="yiheng.chen.mapper" targetProject="src/main/java" type="XMLMAPPER" />

		<!-- tables that needs to be mapped -->
		<table tableName="upms_user" domainObjectName="User"></table>
		<!--<table tableName="book" domainObjectName="Book"></table>-->
	</context>
</generatorConfiguration>